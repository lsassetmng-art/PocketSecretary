<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Phase2 -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <!-- Phase3 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocketSecretary"
        tools:targetApi="31">

        <!-- Launcher -->
        <activity
            android:name=".ui.secretary.SecretaryListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Phase2 -->
        <activity
            android:name=".ui.calendar.CalendarReadActivity"
            android:exported="false" />

        <!-- Phase3 -->
        <activity
            android:name=".ui.notification.NotificationCenterActivity"
            android:exported="false" />

        <receiver
            android:name=".ui.notification.NotificationReceiver"
            android:exported="false" />

        <!-- Phase4 -->
        <activity
            android:name=".ui.billing.BillingStubActivity"
            android:exported="false" />

        <!-- 既存（将来用） -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>
