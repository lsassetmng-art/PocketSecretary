<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🌐 ネットワーク（Supabase / Edge / API） -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 🔔 Android 13+ 通知 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".PocketSecretaryApp"
        android:theme="@style/Theme.PocketSecretary"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true">

        <!-- =============================== -->
        <!-- 🚀 Launcher -->
        <!-- =============================== -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- =============================== -->
        <!-- 🎨 背景選択 -->
        <!-- =============================== -->
        <activity
            android:name=".ui.background.BackgroundSelectActivity"
            android:exported="false" />

        <!-- ⚙ 設定 -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />

        <!-- 📦 アーカイブ -->
        <activity
            android:name=".ui.archive.ArchiveActivity"
            android:exported="false" />

        <!-- 👤 人格選択 -->
        <activity
            android:name=".ui.persona.PersonaSelectActivity"
            android:exported="false" />

        <!-- 🛠 秘書ツール -->
        <activity
            android:name=".ui.tools.SecretaryToolsActivity"
            android:exported="false" />

        <!-- 📅 スケジュール -->
        <activity
            android:name=".ui.secretary.SecretaryListActivity"
            android:exported="false" />

        <!-- 📜 履歴 -->
        <activity
            android:name=".ui.history.HistoryActivity"
            android:exported="false" />

        <!-- 💳 プレミアム -->
        <activity
            android:name=".ui.billing.PremiumActivity"
            android:exported="false" />

        <!-- 🔊 読み上げ -->
        <activity
            android:name=".ui.speech.SpeechToolActivity"
            android:exported="false" />

        <!-- 🔔 手動通知 -->
        <activity
            android:name=".ManualNotificationActivity"
            android:exported="false" />

    </application>

</manifest>
